{% if level == 3 %}
    <h3 class="line-clamp-2 font-semibold text-base sm:text-xl md:text-2xl">{{ title|sup }}</h3>
{% else %}
    <h2 class="line-clamp-2 font-semibold text-base sm:text-xl md:text-2xl">{{ title|sup }}</h2>
{% endif %}

