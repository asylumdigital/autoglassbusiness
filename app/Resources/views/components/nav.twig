{% set children = false %}

{% if nav %}
    <ul class="{{ not isChild ? 'flex md:gap-9 flex-col md:flex-row' : 'space-y-1.5' }}">
        {% for item in nav %}
            {% set children = item.children ?: false %}
            {% set megaClass = '' %}

            {% set megaClass = ' menu-item-mega-menu' %}
            <li class="py-4 border-b md:border-0 level-{{ item.level }} menu-item{{ children ? ' has-children' }}{{ megaClass }}{{ item.current ? ' active md:border-b-4 md:border-brand-secondary' }}{{ not isChild ? ' md:py-3' }}"{{ children ? ' x-data="{ open: false }"' }}>
                <a {% if item.target %}target="{{ item.target }}" {% endif %}href="{{ item.link }}" class="flex items-center gap-1"{{ children ? '@click.stop.prevent="open = !open"' }}>
                    {{ item.title }}
                    {% if children %}
                        <span class="w-6 ml-auto md:hidden">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><path fill="#333" d="M14.804 12.52c.261-.282.261-.75 0-1.062L10.852 7.21c-.29-.281-.726-.281-.988 0l-.668.718c-.261.312-.261.78 0 1.062l2.819 3.029-2.819 2.997c-.261.281-.261.75 0 1.062l.668.687c.262.312.698.312.988 0z"/></svg>
                        </span>
                        <span class="w-2.5 hidden md:block" :class="{ '-scale-y-100': open }">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 11 6"><path stroke="#000" stroke-linecap="round" stroke-linejoin="round" d="M9.571.5 5.036 5.036.5.5"/></svg>
                        </span>
                    {% endif %}
                </a>

                {% if children and item.level == 0 %}
                    <div class="sub-menu-wrapper mega-menu bg-white shadow-2xl absolute top-0 md:top-full md:left-0 md:right-auto w-full font-semibold py-12 transition-all h-full md:h-auto" :class="open ? 'left-0 md:block' : 'left-full md:hidden'" @click.outside="open = false">
                        <div class="container grid md:grid-cols-4">
                            <div>
                                <button type="button" class="md:hidden" @click="open = false">Back</button>
                                <p class="text-3xl mb-4">{{ item.title }}</p>

                                {% if item.link != '#' %}
                                    <p>
                                        <a {% if item.target %}target="{{ item.target }}" {% endif %}href="{{ item.link }}" class="font-light text-sm underline">View all →</a>
                                    </p>
                                {% endif %}
                            </div>
                {% endif %}

                {% include multilevel ? "components/nav.twig" ignore missing with {
                    nav: item.children,
                    class: 'submenu',
                    isChild: true
                }  %}

                {# closing for mega/super menu #}
                {% if item.level == 0 and children %}
                    </div></div>
                {% endif %}
            </li>
        {% endfor %}
    </ul>
{% endif %}
